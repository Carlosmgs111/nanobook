---
export const prerender = true;
export { getStaticPaths } from "../../editor/core/edit-page";
import { loadEditPageProps } from "../../editor/core/edit-page";
import EditPage from "../../editor/EditPage.astro";

const documentId = Astro.props.documentId ?? (Astro.params.slug || "index");
const props = await loadEditPageProps({ documentId });
---

<EditPage {...props} />
