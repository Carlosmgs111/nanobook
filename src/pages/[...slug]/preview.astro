---
export { prerender } from "@/editor/pages/preview.astro";
import PreviewPage from "@/editor/pages/preview.astro";

const slug = Astro.params.slug;
if (!slug) {
  throw new Error("Missing slug");
}

const editHref = `/${slug}/edit`;
---

<PreviewPage slug={slug} editHref={editHref} />
