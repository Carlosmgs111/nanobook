---
export const prerender = false;
import { loadPreviewPageProps } from "../../editor/core/preview-page";
import PreviewPage from "../../editor/PreviewPage.astro";

const documentId = Astro.params.slug || "index";
const props = await loadPreviewPageProps({ documentId });
---

<PreviewPage {...props} />
