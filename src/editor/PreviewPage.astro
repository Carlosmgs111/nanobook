---
interface Props {
  slug: string;
}

const { slug } = Astro.props;
---

<h1><mark>Preview de {slug}</mark></h1>
<div id="preview-container"></div>

<script>
  import {
    ensureRendered,
    getRenderedDocument,
  } from "../rendering/core/render-service";

  const channel = new BroadcastChannel("rendered-document");

  async function renderPreview() {
    const container = document.getElementById("preview-container");
    if (!container) return;

    const rendered = getRenderedDocument();
    if (!rendered) {
      container.innerHTML = "<p>Generando preview...</p>";
      try {
        await ensureRendered();
        const fresh = getRenderedDocument();
        container.innerHTML =
          fresh?.Content ?? "<p>No hay contenido para previsualizar.</p>";
      } catch (error) {
        console.error(error);
        container.innerHTML = "<p>Error al generar el preview.</p>";
      }
      return;
    }

    container.innerHTML = rendered.Content;
  }

  document.addEventListener("astro:page-load", renderPreview);

  channel.addEventListener("message", () => {
    renderPreview();
  });
</script>
