---
import FileSVG from "../../icons/file.svg";
import FolderSVG from "../../icons/folder.svg";
import ToolTip from "./ToolTip.astro";
import type { NavigationNode } from "../../core/navigation/builder";

interface Props {
  entries: NavigationNode[];
}

const { entries } = Astro.props;
---

<ul class="sidebar-list">
  {
    entries.map((page) => (
      <li>
        <a
          href={`/${page.id}/`}
          class="sidebar-link"
          aria-current={page.current ? "page" : undefined}
          data-tooltip={page.title}
        >
          <ToolTip id={"tooltip-" + page.id}>
            <i class="sidebar-icon" slot="icon">
              {page.isIndex ? <FolderSVG /> : <FileSVG />}
            </i>
            <span class="sidebar-text" slot="label" title={page.title}>
              {page.title}
            </span>
          </ToolTip>
        </a>
      </li>
    ))
  }
</ul>

{
  entries.length === 0 && (
    <p class="sidebar-empty">No hay elementos en esta sección.</p>
  )
}

<style>
  .sidebar-list {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    width: 100%;
  }

  :global(.tooltip-container) {
    padding: 0.5rem;
  }

  .sidebar-link {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    border-left: 2px solid transparent;
    font-size: 0.875rem;
    line-height: 1.25rem;
    width: 100%;
    min-width: 0;
    color: var(--muted);
    position: relative;
    z-index: 10;
  }

  .sidebar-link:hover {
    border-color: var(--grid-line-strong);
    background: rgba(255, 255, 255, 0.05);
    color: var(--text);
  }

  .sidebar-link[aria-current="page"] {
    border-color: var(--accent);
    background: rgba(255, 255, 255, 0.05);
    color: var(--text);
  }

  .sidebar-icon {
    display: flex;
    width: 1.25rem;
    flex-shrink: 0;
    justify-content: center;
  }

  .sidebar-text {
    display: block;
    width: 100%;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    min-width: 0;
  }

  .sidebar-empty {
    font-size: 0.875rem;
    line-height: 1.25rem;
    color: var(--muted);
  }
</style>
